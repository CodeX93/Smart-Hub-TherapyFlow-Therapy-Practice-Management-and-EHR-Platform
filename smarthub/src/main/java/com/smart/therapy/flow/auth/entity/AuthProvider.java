package com.smart.therapy.flow.auth.entity;

/**
 * Authentication provider. For MFA/SSO later (LOCAL, GOOGLE, AZURE, OKTA).
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE,
    AZURE,
    OKTA
}
