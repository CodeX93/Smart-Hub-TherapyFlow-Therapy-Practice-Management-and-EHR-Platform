import PageHeader from "@/components/shared/PageHeader";

const UserDashboard = () => {
  return (
    <div>
      <PageHeader
        title="Dashboard"
        description="Quick access to user summaries, tasks, and records."
      />
      <div>User Dashboard</div>
    </div>
  );
};

export default UserDashboard;
